<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.mapper.EnrollmentMapper">
    
    <resultMap id="BaseResultMap" type="com.training.entity.Enrollment">
        <id column="id" property="id"/>
        <result column="training_id" property="trainingId"/>
        <result column="user_id" property="userId"/>
        <result column="status" property="status"/>
        <result column="enrolled_at" property="enrolledAt"/>
        <result column="cancelled_at" property="cancelledAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM training_enrollments WHERE id = #{id} AND deleted = 0
    </select>
    
    <select id="selectByTrainingId" resultMap="BaseResultMap">
        SELECT * FROM training_enrollments 
        WHERE training_id = #{trainingId} AND status = 'enrolled' AND deleted = 0
        ORDER BY enrolled_at DESC
    </select>
    
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM training_enrollments 
        WHERE user_id = #{userId} AND status = 'enrolled' AND deleted = 0
        ORDER BY enrolled_at DESC
    </select>
    
    <select id="selectByTrainingAndUser" resultMap="BaseResultMap">
        SELECT * FROM training_enrollments 
        WHERE training_id = #{trainingId} AND user_id = #{userId} AND status = 'enrolled' AND deleted = 0
        LIMIT 1
    </select>
    
    <select id="countByTrainingId" resultType="int">
        SELECT COUNT(*) FROM training_enrollments 
        WHERE training_id = #{trainingId} AND status = 'enrolled' AND deleted = 0
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO training_enrollments (training_id, user_id, status, enrolled_at, created_at, updated_at, deleted)
        VALUES (#{trainingId}, #{userId}, #{status}, #{enrolledAt}, #{createdAt}, #{updatedAt}, #{deleted})
    </insert>
    
    <update id="updateById">
        UPDATE training_enrollments SET
            status = #{status},
            cancelled_at = #{cancelledAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <update id="deleteById">
        UPDATE training_enrollments SET deleted = 1 WHERE id = #{id}
    </update>
    
</mapper>

