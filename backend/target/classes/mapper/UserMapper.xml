<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.mapper.UserMapper">
    
    <resultMap id="BaseResultMap" type="com.training.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="name" property="name"/>
        <result column="employee_no" property="employeeNo"/>
        <result column="dept" property="dept"/>
        <result column="phone" property="phone"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="password" property="password"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM users WHERE id = #{id} AND deleted = 0
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM users WHERE deleted = 0 ORDER BY created_at DESC
    </select>
    
    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT * FROM users WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (name LIKE '%' || #{keyword} || '%'
            OR employee_no LIKE '%' || #{keyword} || '%'
            OR dept LIKE '%' || #{keyword} || '%')
        </if>
        ORDER BY created_at DESC
    </select>
    
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM users WHERE username = #{username} AND deleted = 0
    </select>
    
    <select id="selectByEmployeeNo" resultMap="BaseResultMap">
        SELECT * FROM users WHERE employee_no = #{employeeNo} AND deleted = 0
    </select>
    
    <select id="countByEmployeeNo" resultType="int">
        SELECT COUNT(*) FROM users 
        WHERE employee_no = #{employeeNo} AND deleted = 0
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, name, employee_no, dept, phone, role, status, password, created_at, updated_at, deleted)
        VALUES (#{username}, #{name}, #{employeeNo}, #{dept}, #{phone}, #{role}, #{status}, #{password}, #{createdAt}, #{updatedAt}, #{deleted})
    </insert>
    
    <update id="updateById">
        UPDATE users SET
            name = #{name},
            employee_no = #{employeeNo},
            dept = #{dept},
            phone = #{phone},
            status = #{status},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <update id="deleteById">
        UPDATE users SET deleted = 1 WHERE id = #{id}
    </update>
    
</mapper>

