<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.mapper.TrainingMapper">
    
    <resultMap id="BaseResultMap" type="com.training.entity.Training">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="trainer" property="trainer"/>
        <result column="location" property="location"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="cover_url" property="coverUrl"/>
        <result column="status" property="status"/>
        <result column="need_signup" property="needSignup"/>
        <result column="need_checkout" property="needCheckout"/>
        <result column="late_minutes" property="lateMinutes"/>
        <result column="early_leave_minutes" property="earlyLeaveMinutes"/>
        <result column="max_participants" property="maxParticipants"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM trainings WHERE id = #{id}
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM trainings ORDER BY created_at DESC
    </select>
    
    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT * FROM trainings
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE '%' || #{keyword} || '%' OR trainer LIKE '%' || #{keyword} || '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>
    
    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM trainings WHERE status = #{status} ORDER BY created_at DESC
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO trainings (title, description, trainer, location, start_time, end_time, cover_url, status,
            need_signup, need_checkout, late_minutes, early_leave_minutes, max_participants,
            created_at, updated_at)
        VALUES (#{title}, #{description}, #{trainer}, #{location}, #{startTime}, #{endTime}, #{coverUrl}, #{status},
            #{needSignup}, #{needCheckout}, #{lateMinutes}, #{earlyLeaveMinutes}, #{maxParticipants},
            #{createdAt}, #{updatedAt})
    </insert>
    
    <update id="updateById">
        UPDATE trainings SET
            title = #{title},
            description = #{description},
            trainer = #{trainer},
            location = #{location},
            start_time = #{startTime},
            end_time = #{endTime},
            cover_url = #{coverUrl},
            status = #{status},
            need_signup = #{needSignup},
            need_checkout = #{needCheckout},
            late_minutes = #{lateMinutes},
            early_leave_minutes = #{earlyLeaveMinutes},
            max_participants = #{maxParticipants},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM trainings WHERE id = #{id}
    </delete>

    <select id="selectCountByMonth" resultType="int">
        SELECT COUNT(*)
        FROM trainings
        WHERE start_time >= DATE_TRUNC('month', CURRENT_DATE)
        AND start_time &lt; DATE_TRUNC('month', CURRENT_DATE) + INTERVAL '1 month'
    </select>
    <select id="selectByMonth" resultType="java.lang.Long">

            SELECT id
            FROM trainings
            WHERE
            start_time >= DATE_TRUNC('month', CURRENT_DATE)
            AND start_time &lt; DATE_TRUNC('month', CURRENT_DATE) + INTERVAL '1 month'

    </select>
    <select id="selectMonthlyTrainingHours" resultType="double">
        SELECT
        COALESCE(
        SUM(EXTRACT(EPOCH FROM (end_time - start_time)) / 3600),
        0.0
        )
        FROM trainings
        WHERE
        start_time >= DATE_TRUNC('month', CURRENT_DATE)
        AND start_time &lt;DATE_TRUNC('month', CURRENT_DATE) + INTERVAL '1 month'
        AND status = 'ended'
        AND end_time IS NOT NULL
        AND end_time > start_time
    </select>
</mapper>

